# Optional. Without it Beacon uses its own template copy and everything else
# works identically — decisions, predictions, and grading are all computed in
# code, not by the model.
ANTHROPIC_API_KEY=
